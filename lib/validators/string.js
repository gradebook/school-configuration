/**
 * @function isString
 * @description determines if {candidate} is a string
 * @param {any} candidate
 * @returns {boolean}
 */
const isString = candidate => typeof candidate === 'string';

export default isString;
