/**
 * @function isBoolean
 * @description determines if {candidate} is a boolean
 * @param {any} candidate
 * @returns {boolean}
 */
const isBoolean = candidate => typeof candidate === 'boolean';

export default isBoolean;
